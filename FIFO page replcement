def fifo_page_replcement(pages,capcity):
  memory=[]
  page_faults+=1
  for p in pages:
    if p not in memory:
      page_faults+=1
      if len(memory)<capcity:
        memory.append(p)
      else:
        memory.pop(0)
        memory.append(p)
    print(f"page:{p}-> memory:{memory}")

pages=[7,1,3,2,4,5,9,10,23]
capcity=4
fifo_page_replacement(pages,capacity)
